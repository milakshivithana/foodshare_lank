// Kept as a reusable listing card for hackathon-speed UI development.
import {MapPin,Clock,Package,ArrowRight} from 'lucide-react';import {Link} from 'react-router-dom';import StatusBadge from './StatusBadge';import {dateOnly} from '../utils/format';
export default function DonationCard({donation}){return <article className="donation-card"><div className="card-top"><span className="category-chip">{donation.category}</span><StatusBadge status={donation.status}/></div><h3>{donation.title}</h3><p className="muted line-clamp">{donation.description}</p><div className="card-meta"><span><Package size={16}/>{donation.quantity} {donation.unit}</span><span><MapPin size={16}/>{donation.city}, {donation.district}</span></div><div className="card-bottom"><span className="expiry"><Clock size={15}/> Best before {dateOnly(donation.bestBeforeDate)}</span><Link className="icon-link" to={`/donations/${donation._id}`}>View details <ArrowRight size={16}/></Link></div></article>}
