import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {Search,HeartHandshake,Clock,CheckCircle2,ArrowRight,XCircle} from 'lucide-react';import api from '../services/api';import Loading from '../components/Loading';import StatusBadge from '../components/StatusBadge';import {dateTime} from '../utils/format';
export default function RecipientDashboard(){const [requests,setRequests]=useState([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=()=>api.get('/requests/my').then(r=>setRequests(r.data.data.requests)).catch(e=>setError(e.response?.data?.message||'Unable to load requests')).finally(()=>setLoading(false));useEffect(load,[]);const cancel=async id=>{if(!confirm('Cancel this pending request?'))return;try{await api.delete(`/requests/${id}`);load()}catch(e){alert(e.response?.data?.message||'Unable to cancel request')}};if(loading)return <Loading text="Loading your recipient dashboard..."/>;const count=s=>requests.filter(r=>r.status===s).length;return <div className="dashboard-page"><div className="container"><div className="dash-header"><div><span className="eyebrow">Recipient workspace</span><h1>Find support that fits.</h1><p>Discover available surplus food and keep your collection requests on track.</p></div><Link className="btn btn-primary" to="/donations"><Search size={18}/> Find food</Link></div>{error&&<div className="form-error">{error}</div>}<div className="stats-grid four"><Stat label="Available donations" value="Browse" icon={<Search/>}/><Stat label="Pending requests" value={count('PENDING')} icon={<Clock/>}/><Stat label="Accepted" value={count('ACCEPTED')} icon={<HeartHandshake/>}/><Stat label="Completed" value={count('COMPLETED')} icon={<CheckCircle2/>}/></div><section className="dash-section"><div className="section-title"><div><h2>My requests</h2><p>Track every donation you have requested.</p></div><Link to="/donations" className="text-link">Browse donations <ArrowRight size={16}/></Link></div>{requests.length?<div className="table-wrap"><table><thead><tr><th>Donation</th><th>Donor</th><th>Quantity</th><th>Pickup</th><th>Status</th><th>Actions</th></tr></thead><tbody>{requests.map(r=><tr key={r._id}><td><b>{r.donationId?.title}</b><small>{r.donationId?.city}</small></td><td>{r.donorId?.organizationName||r.donorId?.name}</td><td>{r.requestedQuantity} {r.donationId?.unit}</td><td>{dateTime(r.preferredPickupTime)}</td><td><StatusBadge status={r.status}/></td><td>{r.status==='PENDING'&&<button className="table-btn" onClick={()=>cancel(r._id)}><XCircle size={15}/> Cancel</button>}</td></tr>)}</tbody></table></div>:<div className="empty"><HeartHandshake size={30}/><h3>No requests yet</h3><p>Find an available donation and send your first request.</p><Link className="btn btn-primary" to="/donations">Find food</Link></div>}</section></div></div>}
function Stat({label,value,icon}){return <div className="stat-card"><div className="stat-icon">{icon}</div><div><strong>{value}</strong><span>{label}</span></div></div>}
