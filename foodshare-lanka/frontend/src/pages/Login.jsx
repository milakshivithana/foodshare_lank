import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {LogIn,ArrowRight} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Login(){const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {login}=useAuth();const nav=useNavigate();const loc=useLocation();const submit=async e=>{e.preventDefault();setError('');setBusy(true);try{const r=await login(form);nav(loc.state?.from||`/dashboard/${r.data.user.role.toLowerCase()}`)}catch(e){setError(e.response?.data?.message||'Unable to sign in. Please try again.')}finally{setBusy(false)}};return <AuthShell title="Welcome back" subtitle="Sign in to manage donations, requests and community support."><form onSubmit={submit} className="form"><Field label="Email" type="email" value={form.email} onChange={v=>setForm({...form,email:v})} placeholder="you@example.com" required/><Field label="Password" type="password" value={form.password} onChange={v=>setForm({...form,password:v})} placeholder="Your password" required/>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary full" disabled={busy}>{busy?'Signing in...':<>Sign in <ArrowRight size={18}/></>}</button></form><p className="auth-foot">Don't have an account? <Link to="/register">Create one</Link></p><div className="demo-hint"><b>Demo accounts</b><span>donor@example.com · recipient@example.com · admin@example.com</span><small>Password: Demo12345!</small></div></AuthShell>}
function AuthShell({title,subtitle,children}){return <div className="auth-page"><div className="auth-card"><div className="auth-mark"><LogIn/></div><span className="eyebrow">FoodShare Lanka</span><h1>{title}</h1><p>{subtitle}</p>{children}</div></div>}function Field({label,type='text',value,onChange,placeholder,required}){return <label className="field"><span>{label}{required&&' *'}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} required={required}/></label>}
