# FoodShare Lanka API

Express + MongoDB/Mongoose REST API for the FoodShare Lanka MVP.

Run locally:

```bash
npm install
cp .env.example .env
npm run seed
npm run dev
```

Health check: `GET /api/health`.
